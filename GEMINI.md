# GEMINI.md

Use `AGENTS.md` as the canonical repository guide.

Then read `docs/ai/context.md` for product, design, and Linear workflow context.

## Extra guidance

- Keep changes focused.
- Respect generated files.
- Do not commit secrets.
- Follow `Design.md` for UI work.
- Prefer `npm run build` over broad lint cleanup.
