SUPABASE_PUBLISHABLE_KEY=
SUPABASE_URL=
VITE_APP_URL=
VITE_NATIVE_AUTH_REDIRECT_URL=
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=

# Optional — enables client-side error capture. Leave blank in local dev.
VITE_SENTRY_DSN=

# Server-side only. Required by:
#   - scripts/seed-dev.mjs (local seeding)
#   - the /api/ical/:token endpoint (calendar subscription feed)
# Never ship this to the browser and never commit it.
SUPABASE_SERVICE_ROLE_KEY=
